services:
  authentik_proxy:
    hostname: auth-outpost
    container_name: auth-outpost
    image: ghcr.io/goauthentik/proxy:2025.6.4@sha256:28921cce42aebc2a8c7dfc1de0b46d2dde91fa7ba1c271ca6350a8bd07327550
    environment:
      AUTHENTIK_HOST: https://auth.fzymgc.house
      AUTHENTIK_INSECURE: "false"
      # AUTHENTIK_TOKEN must be set in .env
    networks:
      - main
      - internal
    env_file: .env
    volumes:
      - /etc/ssl/certs:/etc/ssl/certs
networks:
  main:
    name: main-bridge
    external: true
  internal:
    name: internal-bridge
    external: true
